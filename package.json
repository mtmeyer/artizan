{
  "name": "artizan",
  "version": "0.0.3",
  "description": "Tiny CSS in JS library focusing on CSS custom properties for theming",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && esbuild src/*.ts --outdir=lib --sourcemap"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nanoid": "^3.3.2"
  },
  "devDependencies": {
    "esbuild": "^0.14.36",
    "typescript": "^4.6.3"
  }
}
